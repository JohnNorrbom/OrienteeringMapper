<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.OrienteeringMapper" parent="Theme.Material3.DayNight.NoActionBar">
    <!-- Required for edge-to-edge to look good -->
    <item name="android:statusBarColor"  type="color">@android:color/transparent</item>
    <item name="android:navigationBarColor"  type="color">@android:color/transparent</item>
    </style>
</resources>